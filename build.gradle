apply plugin: 'java'

sourceCompatibility = 1.5
version = '1.0'

repositories {
    mavenCentral()
}

sourceSets {
    main.java.srcDir "src/"
}

jar {
    manifest {
        attributes(
            'Main-Class': 'com.ach.achViewer.Main'
        )
    }
}